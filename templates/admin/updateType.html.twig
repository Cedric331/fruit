{% extends 'base.html.twig' %}

{% block title %}
{% if(isModification) %}
Modification Type
{% else %}
Création d'un Type
{% endif %}
{% endblock %}

{% block titre %}
{% if(isModification) %}
Modification Type
{% else %}
Création d'un Type
{% endif %}
{% endblock %}

{% block body %}

<div class="container">
   {{ form_start(form) }}
   {% if(isModification) %}
      <img src="{{ asset('/image/types/' ~ type.image)}}" style="width:20%;" >
   {% endif %}
   {{ form_row(form.imageFile) }}
   {{ form_row(form.libelle) }}

   <input class="btn btn-primary" type="submit" value="Valider">

   {{ form_end(form) }}
</div>
{% endblock %}
